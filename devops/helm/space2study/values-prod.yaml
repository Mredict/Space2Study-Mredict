namespace: space2study-prod

image:
  registry: "<AWS_ACCOUNT_ID>.dkr.ecr.eu-central-1.amazonaws.com"

frontend:
  replicas: 2
  image:
    tag: latest # overwritten by Jenkins's "Update GitOps Manifest" stage on prod builds
backend:
  replicas: 2
  image:
    tag: latest # overwritten by Jenkins's "Update GitOps Manifest" stage on prod builds

mongodb:
  replicas: 3

externalSecrets:
  appSecretsName: space2study-app-secrets-prod
